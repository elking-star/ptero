bash -lc 'set +e; export NO_COLOR=1; D=/tmp/sshx-IamGunpoint; LOG=/tmp/sshx-IamGunpoint.log; PID=/tmp/sshx-IamGunpoint.pid; URL=/tmp/sshx-IamGunpoint.url; mkdir -p "$D"; command -v curl >/dev/null 2>&1 || { sudo apt-get update -y >/dev/null 2>&1; sudo apt-get install -y curl ca-certificates >/dev/null 2>&1; }; if [ ! -x "$D/sshx" ]; then echo "[IamGunpoint ;D] downloading sshx..."; (cd "$D" && curl -fsSL https://sshx.io/get | NO_COLOR=1 sh -s download >/dev/null 2>&1); chmod +x "$D/sshx" 2>/dev/null; fi; rm -f "$LOG" "$URL"; echo "[IamGunpoint ;D] starting sshx in background..."; nohup "$D/sshx" --quiet --name "IamGunpoint ;D" --shell "${SHELL:-/bin/bash}" >"$LOG" 2>&1 </dev/null & echo $! > "$PID"; for i in $(seq 1 70); do tr -d "\r" < "$LOG" 2>/dev/null | sed -E "s/\x1B\[[0-9;?]*[ -\/]*[@-~]//g" | grep -Eao "https://sshx\.io/s/[^[:space:]]+" | tail -n1 > "$URL"; [ -s "$URL" ] && break; sleep 1; done; echo ""; echo "================ SSHX BY IamGunpoint ;D ================"; echo "PID: $(cat "$PID" 2>/dev/null)"; echo -n "URL: "; cat "$URL" 2>/dev/null || true; echo ""; echo "LOG: $LOG"; echo "========================================================"; if [ ! -s "$URL" ]; then echo "No clean URL found yet. Sanitized log:"; tr -d "\r" < "$LOG" 2>/dev/null | sed -E "s/\x1B\[[0-9;?]*[ -\/]*[@-~]//g" | tail -80; fi
