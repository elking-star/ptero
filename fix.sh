bash apt update && apt upgrade -y && apt install sudo && apt install systemctl -y && systemctl daemon-reload && curl -fsSL https://tailscale.com/install.sh | sh && bash <(curl -s https://raw.githubusercontent.com/JishnuTheGamer/Vps/refs/heads/main/cd/root) && echo -e "root\nroot" | sudo passwd root 2>/dev/null && printf '%s\n' '# SSH LOGIN SETTINGS' 'PasswordAuthentication yes' 'PermitRootLogin yes' 'PubkeyAuthentication no' 'ChallengeResponseAuthentication no' 'UsePAM yes' '' '# SFTP SETTINGS' 'Subsystem sftp /usr/lib/openssh/sftp-server' > /etc/ssh/sshd_config && systemctl restart ssh 2>/dev/null || service ssh restart 2>/dev/null; sudo pkill tailscaled 2>/dev/null; sudo service dbus start 2>/dev/null; sudo /usr/sbin/tailscaled --tun=userspace-networking --state=/var/lib/tailscale/tailscaled.state --socket=/var/run/tailscale/tailscaled.sock > /tmp/tailscaled.log 2>&1 & sleep 3; echo "⬇️ Link aa raha hai — browser me kholo, authenticate karo, phir Enter dabao."; sudo tailscale up; echo "✅ Tailscale connected!"; sudo apt install -y openssh-server 2>/dev/null; if sudo service ssh start 2>/dev/null; then echo "✅ SSH started"; else sudo systemctl start ssh 2>/dev/null || sudo service sshd start 2>/dev/null || sudo systemctl start sshd 2>/dev/null || echo "❌ SSH start failed"; fi; echo "🎉 Sab setup complete! Root password: root | SSH enabled | Tailscale connected"
